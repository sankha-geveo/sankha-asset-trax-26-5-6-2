# Toggle mock mode for local development
VITE_MOCK_MODE=true

# Comma-separated HotPocket server WebSocket URLs (used when VITE_MOCK_MODE=false)
# Example: wss://localhost:8081
VITE_CONTRACT_URLS=wss://localhost:8081
